document.addEventListener('DOMContentLoaded', function () {
    // Simulate content loading (replace with your actual content loading logic)
    setTimeout(function () {
      // Content is loaded, remove the loading state
      document.body.classList.add('loaded');
    }, 2000); // Adjust the timeout based on your content loading time
  });